# lazy107 作品简介

> 面向中国科大 107 高性能计算平台（Slurm）的**集群端作业提交命令行智能体**。
> 一句话：把「手写 sbatch、反复配 conda 环境、看不懂失败日志」这三件高频易错的事，
> 变成「三条命令 + 一个问答向导」——项目目录进去，成功提交的作业出来。

## 一、作品背景

107 计算平台承载教学、科研与竞赛三类场景：深度学习课程实验、课题组模型训练、
算力竞赛评测。但平台的真实使用门槛并不在算力本身，而在**使用方式**：

1. **Slurm 知识密集**：一次提交要正确组合 account / partition / QoS / 资源申请
   四个知识域，组合错误即报 `Invalid account or account/partition combination`
   ——这是平台上出现频率最高的报错之一，而「申请多少卡、多少内存、多长时间」
   几乎只能靠试错。
2. **环境配置重复劳动**：每个项目都要新建 conda 环境、重装 torch；且 conda 默认
   源装出的 torch 是 **CPU 版**——与申请的 GPU 不匹配时作业会**静默退化为 CPU
   训练**，跑完才发现结果不对，浪费宝贵的 GPU 配额。
3. **失败诊断门槛高**：作业失败后要自己对照 sacct 退出码、翻日志、搜 traceback，
   初学者往往无从下手。
4. **没有统一工作流**：提交、结果、历史都散落在各处，换个人或换个项目就得重新
   来一遍。

lazy107 由此而生：把「领域专家 + 平台老用户」的知识（Slurm 参数语义、退出码含义、
真实失败日志形态、平台权限规则）固化为**可解释的本地规则引擎**，让用户只面对
「我的项目是什么」，不面对「平台要我填什么」。

## 二、解决的问题（场景对应）

| 场景 | 具体问题 | lazy107 的应对 |
|---|---|---|
| 教学（课程实验） | 学生不懂 Slurm，实验课第一周全耗在报错上 | `everything` 问答向导 + 5 档资源预设（CPU/GPU/多卡），回车即用当前值，全程无需学 Slurm |
| 科研（模型训练） | 反复配环境、申请了 GPU 却在 CPU 上跑、作业超时/OOM 不知道原因 | 环境自动准备与**复用校验**；「GPU 即 CUDA」四道防线；`debug` 一行输出 原因/修法/证据 |
| 竞赛（算力评测） | 竞赛账户的分区/QoS 与默认值不兼容，首次提交必被拒 | `discover` 从实时 Slurm 关联解析出你有权使用的 account/partition/QoS，一次解决 |
| 服务管理（日常运维） | 作业状态、日志位置、历史记录全靠人记 | 提交自动记账（`notes/runs.md` + `notes/history.md`），`check` 随时显示进度与下一步 |

## 三、核心功能

| 功能模块 | 命令 | 作用 |
|---|---|---|
| 平台接入 | `discover` | 解析账户权限（account/partition/QoS）写入用户配置，解决最典型的提交被拒 |
| 项目脚手架 | `init` | 内置模板一键生成带 torch 依赖的完整项目骨架 |
| 代码级感知 | `plan`、`env` | 自动检测入口文件；扫描代码 import 推断依赖与资源；**复用优先**地准备 conda 环境；识别 DDP 代码自动切换 torchrun 启动 |
| 确定性生成 | `plan`、`render` | 五层配置合并为唯一事实来源，纯函数渲染 sbatch；`--dry-run` 看到的即将要提交的 |
| 一键提交 | `submit`、`everything` | 校验 → 预检 → 渲染 → 确认 → 提交 → 记账 的 12 步流水线；向导版把全流程收敛为问答 |
| 失败诊断 | `debug` | sacct 退出码语义 + 从真实 107 日志采集的失败签名库，「最具体优先」匹配，输出 cause/fix/evidence |
| 监控与记录 | `watch`/`status`/`logs`/`check` | 监控命令生成、作业状态、日志查看、工作流仪表盘；全程记账 |
| 数据搬运辅助 | `transfer` | 发现大文件并打印「打包 → GUI 上传 → 解包 → sha256 校验」清单（只打印、不执行） |

共 14 个子命令；完整命令表见 [README](README.md)。

## 四、使用的模型与 API 调用方式

**模型：本作品不使用大语言模型，不调用任何外部或在线 API，零网络依赖。**
所有「智能」行为（依赖推断、入口检测、权限解析、失败诊断、资源建议）均由
**本地确定性的规则引擎**实现：

- 依赖与资源推断：代码 import 扫描（含 sklearn→scikit-learn 等名称修正映射）+ 二元启发式；
- 平台权限解析：`sacctmgr`/`scontrol` 机器格式输出的确定性打分；
- 失败诊断：sacct 退出码语义表 + 正则签名匹配库（从真实日志采集、按最具体优先排序）；
- 配置决策：五层配置的固定优先级合并。

**API 调用方式：** 工具与平台的全部交互，是**以子进程方式调用集群标准命令行接口**：

| 接口 | 用途 |
|---|---|
| `sacctmgr` / `scontrol` | 账户-分区-QoS 关联解析、分区上限读取（`--parsable2 --noheader` 机器格式） |
| `sinfo` / `sbatch` / `sacct` / `squeue` | 作业生命周期：预检、提交、状态查询 |
| `conda` / `pip` / `python` | 环境创建、依赖安装（PyPI 默认源锁定 CUDA 版）、CUDA 构建校验 |

所有调用均可打印、带超时、失败信息透传，且登录节点只做管理、训练永远走 `sbatch`。

这一设计带来三个直接收益：**结果可复现**（同样的输入永远得到同样的输出）、
**行为可审计**（每一步都能解释为什么）、**零成本运行**（无 token 消耗、无隐私
外泄风险、评测环境无需联网即可完整运行）。

## 五、创新点

1. **面向真实失败样本的签名式诊断**：不用大模型做「看似聪明」的猜测，而是把
   实机采集的失败形态编码为最具体优先的签名库，配合 sacct 退出码语义表，输出
   确定性、可执行的 原因/修法/证据 三行——诊断结论可离线单测（274 个用例中
   专门的诊断用例全覆盖）。
2. **「GPU 即 CUDA」四道防线**：安装锁定（pip 默认源）→ 装后校验
   （`torch.version.cuda`）→ 提交前拦截 → 运行时守卫，从根上杜绝「申请了 GPU
   却在 CPU 上跑」这类最难察觉的资源浪费与结果错误。
3. **单一事实来源 + 确定性渲染**：RunPlan 冻结数据模型 + 纯函数渲染，使
   `--dry-run` 的预览与真实提交产物**逐字节一致**——「预览可信」从架构上保证，
   而非靠承诺。
4. **代码级感知，零配置可用**：不读任何配置文件也能扫描 import 推断依赖、
   自动检测入口、识别 DDP；配置文件只是覆盖项，永不成为必需品。
5. **可解释智能的工程化**：平台行为（权限矩阵、日志格式演进、`--mem` 实测不
   强制执行）全部实机采集、记录在案并写进测试——工具的知识库随平台演化，不随
   模型幻觉漂移。
6. **轻到极致的可部署性**：零运行时第三方依赖、纯标准库（Python ≥ 3.11），
   单机可测、274 个用例全离线；安装/卸载各两条命令，评测环境 5 分钟即可跑通。

## 六、配套材料

| 材料 | 位置 |
|---|---|
| 设计文档（设计思路/架构/模块/技术难度） | [docs/design.md](docs/design.md) |
| 分步教程 | [docs/quickstart.md](docs/quickstart.md) |
| 端到端演示（ResNet-18/CIFAR-10 教学项目逐步走通） | [docs/demo.md](docs/demo.md) |
| 实机演示合集 | [docs/demos/](docs/demos/INDEX.md) |
| 部署包（可执行程序文件） | `lazy107-0.1.0.tar.gz`（内含离线安装说明） |
| 演示视频 | 另附（≤5 分钟） |

## 快速体验（5 条命令）

```bash
lazy107 discover                 # 一次性解析你有权使用的分区/QoS
lazy107 init my-project && cd my-project
lazy107 env --yes                # 自动扫描 import 并准备 conda 环境
lazy107 plan                     # 查看将提交什么（可先 --dry-run 预览）
lazy107 submit --yes             # 提交并自动记账
```
